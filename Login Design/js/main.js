var contenedor_form = document.querySelector(".contenedor-form")
var toggle_ = document.querySelector(".toggle")
var formulario_ = document.querySelector(".formulario")


// animación con js para Registarse
$('.toggle').click(function () {
    $('.formulario').animate({
        height: "toggle",
        'padding-top': 'toggle',
        'padding-bottom': 'toggle',
        opacity: 'toggle'
    }, "slow");
});

function() {
    
}
